{
  "X-API": {
    "description": "twitterapi.io 上获取的 API key",
    "type": "string"
  },
  "TARGET_ACCOUNT": {
    "description": "要监控的推特账号，默认为 Blue_ArchiveJP",
    "type": "string",
    "default": "Blue_ArchiveJP"
  },
  "CHECK_INTERVAL": {
    "description": "检查间隔，单位秒，默认 600（10分钟）",
    "type": "integer",
    "default": 600
  },
  "SUBSCRIBE_GROUPS": {
    "description": "要发送通知的订阅群组列表，默认为空列表",
    "type": "list",
    "default": []
  }
}
